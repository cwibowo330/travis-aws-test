general:
  build_dir: travis-aws-test
test:
  override:
    - echo "test"
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync travis-aws-test s3://s3-us-west-1.amazonaws.com/www.cwibowo.com --delete